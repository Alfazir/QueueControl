@model QueueControlServer.ViewModels.QueueItems.QueueItemsListViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1></h1>


@*<table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.OderLine)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CarNumber)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Barecode)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.OderLine)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CarNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Barecode)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.QueueID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.QueueID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.QueueID">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>*@
<p>
    <a asp-action="Create">Регистрация в очереди</a>
</p>

<head>
    <script src="lib/jquery/dist/jquery.min.js" type="text/javascript"></script>
    <link rel="stylesheet" href="lib/bootstrap/dist/css/bootstrap.css" />
    <link rel="stylesheet" href="css/site.css" />
    <script src="js/site.js" type="text/javascript"></script>
</head>

@*
    <body>
        <div class="text-center">
            <p id="result_text"></p>
            <a class="btn btn-info btn-sm"     id="example-4" >Выполнить запрос на 3 элемента</a>
            </br>
            <a class="btn btn-info btn-sm" onclick="LoadProdData()">Выполнить запрос на 10 элементов</a>
            </br>
            <a class="btn btn-info btn-sm" onclick="LoadProdData();">Выполнить запрос на 0 элементов</a>
            </br>
            <a class="btn btn-info btn-sm" onclick="LoadProdData();">Выполнить запрос на 0 элементов</a>
        </div>
    </body>
*@

<form>
    <div class="form-inline form-group">
        <label class="control-label">Поиск: </label>
        <input name="name" class="form-control-lg" id="Search" />
        <label class="control-label"> </label>

        <label class="control-label">Перевозчик: </label>
        <select name="Carriers" id="myCarrier" asp-items="Model.Carriers" class="form-control"> </select>

        <label class="control-label">Завод:  </label>
        <select name="Factorys" id="myFactory" asp-items="Model.Factories" class="form-control"> </select>
        
        

        <label class="control-label">Очередь: </label>
        <select name="Queues" id="myQueue"  class="form-control">
            <option disabled selected value=''>Все</option>


        </select>

        <input type="submit" value="Очистить" class="btn btn-default" id="ddl" onclick="LoadProdData();" />
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>Номер в очереди</th>
            <th>Гос. номер ТС</th>
            <th>Карта водителя</th>
            <th>Очередь</th>
            <th>Ф.И.О водителя</th>
            <th>Перевозчик</th>
            <th>Цементный Завод</th>
            <th>Время регистрации</th>
        </tr>
    </thead>
    <tbody table-striped id="tableBody" bgcolor="#E0FFFF" ></tbody>
</table>
<button></button>
