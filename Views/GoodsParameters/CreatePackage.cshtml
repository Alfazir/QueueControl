@model QueueControlServer.Models.Package

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>CreateBrand</h1>

<h4>GoodsParameter</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreatePackage">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PackageName" class="control-label">PackageName</label>
                <input type="text" asp-for="PackageName" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="CreatePackage" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}
